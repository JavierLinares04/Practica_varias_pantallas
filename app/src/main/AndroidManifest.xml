<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javierlinares.act_videojuego">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Web"></activity>
        <activity android:name=".Modificar" />
        <activity android:name=".Precio_nintendo" />
        <activity android:name=".Precio_Play5" />
        <activity android:name=".Precios_personalizados" />
        <activity android:name=".Nintendo" />
        <activity android:name=".Microsoft" />
        <activity android:name=".Sony" />
        <activity android:name=".Fabricante" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>